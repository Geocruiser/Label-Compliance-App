# Scripts

## OCR Service

- `npm run ocr:up` - build and run Dockerized PaddleOCR service on port `8001`
- `npm run ocr:down` - stop OCR service container

## Benchmark

- `npm run benchmark:p95` - deterministic verification-core latency benchmark (p50/p95/avg/min/max)